<div class="card">
    <div class="nav">
        {{#each links}}
        <div class="nav-item">
            <a href="{{href}}"
                class="nav-link active active {{#if @last includeZero=true}} disabled {{/if}}">{{text}}</a>
        </div>
        {{/each}}
    </div>
</div>